%strong
  Items
%br
- if @box.npc && !@box.npc.stat.dead?
  = "Defeat the #{@box.npc.description.name} to see what's in this room!"
- else
  - if @box.item
    - @box.items.each do |item|
      = "#{item.description.name}: #{item.description.text}"
      %br
      - if @game.player.inventory_full?
        %em
          = "Inventory full..."
      - else
        = link_to "Take: #{item.description.name}", edit_game_path(id: @game.id, game_action: 't', item_id: item.id)
      %br
  - else
    There's nothing useful here...
